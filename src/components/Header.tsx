import React from "react";
import { Link } from "react-router";

const Header = () => {
  return (
    <header>
      <Link to="/timeTablePage">timeTablePage</Link>
    </header>
  );
};

export default Header;
